<!DOCTYPE html>
<html>
  <head>
    <%= stylesheet_link_tag 'parcels', media: 'all', 'data-turbolinks-track': 'reload' %>


    <title>CityGardens</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= stylesheet_link_tag 'plants', media: 'all', 'data-turbolinks-track': 'reload' %>




    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.css" rel="stylesheet">
  </head>

  <body>
    <%#    Very useful when we dont want sth in a specific page (Educational for later)       %>
    <%#    <% unless params[:controller] == "pages" && params[:action] == "home" %>
    <%#    <% end %>
    <%= render "shared/flashes" %>
    <%= yield %>
    <% unless params[:controller] == "pages" && params[:action] == "home" %>
      <%= render "shared/navbar" %>
    <% end %>
  </body>
</html>
